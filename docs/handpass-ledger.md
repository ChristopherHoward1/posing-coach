# Hand-pass Portability Ledger

This repository is the **first hand-instantiation of `agentic-coding-template` onto a
non-bash (Python) project**. This ledger is the empirical record of how the ported
harness meets a Python project: where it fights, and — just as important — where it
ports cleanly.

**What this ledger is and is not.** It records **observations and hypotheses**, not a
mandate to change the template. One hand-pass can show that something is coupled *in this
instance*; it cannot distinguish "coupled in general" from "coupled to Python
specifically" — that needs a second hand-pass to confirm recurrence. Template changes stay
friction-justified (recurrence across hand-passes, `n >= 2`, or measured cost), exactly
like any other earned automation. A row here is evidence, not a spec. The
`template hypothesis` column holds hypotheses, never requirements.

**Axis vocabulary** (why did the artifact fight?):

- `bash-coupling` — needs a per-language rewrite. Expected, routine porting cost.
- `template-design-flaw` — an assumption wrong even for a generic project (e.g. an
  operating doc naming a specific implementation artifact). More valuable to catch: it
  would bite any project, not just Python.
- `machine` — the host lacks something (tool absent, wrong version).
- `shell-env` — environment/layout mismatch (PATH, venv layout, quoting).
- `other` — anything else (note it inline).

Keep `bash-coupling` and `template-design-flaw` distinct: the first is expected porting
cost; the second is a latent template bug.

---

## Part A — Artifact inventory (the denominator)

One row per inherited scaffolding artifact. Clean ports are logged too: an artifact that
needed zero changes is the strongest evidence it belongs in a future template verbatim.

| inherited artifact | verdict | if not clean: what fought + axis | cost & recurrence | template hypothesis (NOT spec) |
| --- | --- | --- | --- | --- |
| `CLAUDE.md` | adapted | Review section hard-references `scripts/review-context.sh` (a bash gate helper that was not ported) → dangling pointer; gate prose is in bash terms. Axis: bash-coupling, with a `template-design-flaw` edge (an operating doc naming a specific script path inherits a dangling reference on any partial port). | one-time, cheap (doc edit in the planning PR) | An operating doc might describe the review gate by *command/role* ("run the repo's lint + test gate") rather than a hard-coded script path, so a port cannot inherit a dangling reference. Hypothesis, n=1. |
| `AGENTS.md` | clean-verbatim | — | none | Language-agnostic role/contract ("tests, lint, typecheck, build" phrased generically). Strong candidate for verbatim template inclusion. |
| `.github/ISSUE_TEMPLATE/implementation.md` | clean-verbatim | — | none | Language-agnostic; already carries `## Dependencies`. Verbatim candidate. Watch: `new-issue.sh` may not render `## Dependencies` (inherited divergence) — confirm when exercised. |
| `.github/PULL_REQUEST_TEMPLATE.md` | clean-verbatim | — | none | Language-agnostic. Verbatim candidate. |
| `scripts/new-issue.sh` | clean-verbatim (ran unmodified) | Renders **no** `## Dependencies` section, so Issue #3's `Depends on #2` edge had to be added via `gh issue edit`. Axis: template-design-flaw (renderer diverged from the issue template). | one-time per dependent issue; recurs. **2nd hand-pass occurrence** of the donor's n=1 Milestone-12 finding. | Renderer should emit `## Dependencies` (+ a `--depends-on` flag). Now n=2 across hand-passes — approaching friction-justification. |
| `scripts/new-handoff.sh` | adapted (fixed in PR #6 / Issue #4) | Repo-root guard aborted on Windows Git Bash: `git rev-parse --show-toplevel` → `C:/Dev/...` but `pwd -P` → `/c/Dev/...`, so `REPO_ROOT != CURRENT_DIR`. Fixed by normalizing via `cd "$(git rev-parse --show-toplevel)" && pwd -P`. Axis: shell-env, with a template-design-flaw edge (a POSIX path assumption baked into the script). | one-time narrow fix, now landed. n=2 (identical check in `trigger-agent.sh`). Dogfooded clean on Issue #3 (created the `feat/m1-pose-keypoints` branch, no Windows abort). | A portable repo-root check (`[ A -ef B ]`, or the `cd … && pwd -P` normalization used here) works on Windows too. Same *class* as the donor's `[[ -d .git ]]` worktree assumption. |
| `scripts/trigger-agent.sh` | adapted (fixed in PR #6 / Issue #4) | Identical repo-root guard as `new-handoff.sh` → same Windows abort; fixed identically. Also rejects a dirty tree (see `.gitignore` row). Separately, when *invoked* it dispatches `codex exec` into a Windows sandbox that is **edit-only** (see Part B) — orthogonal and unfixed. Axis: shell-env / template-design-flaw. | one-time; fix travelled with `new-handoff.sh`. Dogfooded clean on Issue #3 (dispatched `codex exec` ×2, header `sandbox: workspace-write`). | Fix both scripts' repo-root check together (done). The edit-only-sandbox limitation is a separate, unresolved constraint. |
| `PLAN.md` ("skeleton") | discarded+replaced | It was **not** a skeleton — it carried the donor's entire Milestone 1–12 history (all about the harness itself), so a fresh reader would learn nothing about *this* product. Rewritten to posing-coach M1. Axis: `template-design-flaw` (instantiation did not skeletonize PLAN). | one-time, moderate (full rewrite in the planning PR) | A template should ship `PLAN.md` as a blank skeleton (product vision + empty milestone slots), not the donor's history. Hypothesis, n=1. Pairs with the `CLAUDE.md`/`AGENTS.md` clean ports: agnostic operating docs travel; product-specific `PLAN.md` does not. |
| gate — lint: shellcheck → `ruff check` | discarded+replaced (Issue #2, PR #7) | The bash gate lints via a `lint.sh` that globs `scripts/*.sh`; Python replaces it with `ruff check`, which does its own discovery — **no custom script needed**. Axis: bash-coupling. | one-time | `lint.sh` may be bash-ecosystem scaffolding (bash has no native linter/runner), not a universal template need. Hypothesis, n=1. |
| gate — tests: bash harness → `pytest` | discarded+replaced (Issue #2, PR #7) | Bash needs a hand-rolled test runner + discovery loop; `pytest` does discovery natively. Axis: bash-coupling. | one-time | Same as above: the template's test-runner scaffolding may be a bash workaround, not a universal need. Hypothesis, n=1. |
| gate — orchestration: `review-context.sh` lint+test run → `scripts/gate.py` | adapted+replaced (Issue #2, PR #7) | The bash gate bundled lint+test into one read-only pass; the Python equivalent (`scripts/gate.py`) is ~15 lines calling `ruff`/`pytest` because the ecosystem already provides discovery/runners. `review-context.sh`'s **richer** gathering (PR metadata, linked issue, diff) is deliberately **not** ported — that is not the lint+test job. Axis: bash-coupling. | one-time | The orchestration script may be near-vacuous in a real language ecosystem (`ruff && pytest` is nearly as good). Whether a wrapper earns its place is itself the hypothesis; needs a 2nd hand-pass. n=1. |
| gate — `pyproject.toml` | new (Issue #2, PR #7) | No bash analog; Python needs its own project/config file (ruff + pytest config). Axis: bash-coupling. | one-time | Every Python port needs a `pyproject.toml`; a Python-flavored template would ship a minimal one. Hypothesis, n=1. |
| `.gitignore` | adapted (Issue #2, PR #7) | Inherited `.gitignore` ignores only `.venv/`. Python runs create `__pycache__/`, `.pytest_cache/`, `.ruff_cache/`, which dirty the tree — and `new-handoff.sh`/`trigger-agent.sh` **reject a dirty tree**. So the Python gate couples to the workflow scripts' clean-tree check. Axis: bash-coupling / other (language artifacts). | one-time, cheap | A Python template ships a Python `.gitignore`; the clean-tree coupling is real and language-independent in *shape* (every language has build/cache artifacts). Hypothesis, n=1. |
| `requirements.txt` | pre-supplied (not template scaffolding) | Product deps (mediapipe, opencv, numpy, …) + dev tools (ruff, pytest) were committed by the Product Owner, not inherited from the template. | — | Out of template scope; noted for completeness. |

---

## Part B — Event log (chronological)

Append the moment friction occurs, so nothing is lost between orientation and review.
Findings fold back into Part A.

| when (milestone/step) | what happened | axis | resolution |
| --- | --- | --- | --- |
| M1 / Step 1 orient | `scripts/` ships only the three workflow scripts; `lint.sh` and `review-context.sh` were not ported, yet `CLAUDE.md`'s Review section still instructs running `scripts/review-context.sh` → dangling reference. | bash-coupling + template-design-flaw | Recarve the gate to Python (Issue A); fix the `CLAUDE.md` Review prose in the planning PR. |
| M1 / Step 1 orient | `PLAN.md` contained the donor template's full Milestone 1–12 history, not a posing-coach skeleton. | template-design-flaw | Gutted and rewritten to M1 in the planning PR. |
| M1 / Step 1 orient | ruff/pytest/python are not on the global PATH; they live in `.venv/Scripts/*.exe` (Windows venv layout, not POSIX `.venv/bin`). | shell-env / machine | Gate and all verification run with the venv active; recorded as a key constraint in the gate handoff. |
| M1 / Step 1 orient | `mediapipe 0.10.35` imports cleanly (with `cv2`) in the venv → M1 is feasible offline **iff** the bundled `mediapipe.solutions.pose` API is used (the Tasks API needs a downloaded `.task` model). | other (product-dep) | Constrain Issue B to the legacy `solutions.pose` API for hermeticity. **⚠ LATER FALSIFIED** (see "M1 / product orient" below): this build has no `solutions` module at all — the claim was recorded from a top-level `import`, never from executing `mediapipe.solutions.pose`. |
| M1 / Step 3 plan | `pytest` exits 5 on "no tests collected", so a gate treating non-zero as failure would fail on the empty bootstrap tree (the gate PR precedes the first test). This parallels the donor's bash empty-array/`nullglob` fights (Milestones 5, 9) — same *bootstrap-empty-suite* category, different mechanism. | bash-coupling | `gate.py` treats pytest exit 5 as non-failing; pinned in Issue A acceptance. |
| M1 / Step 4 decompose | `new-issue.sh` (dry-run + real) created Issues #2/#3 cleanly, but rendered no `## Dependencies`; added `Depends on #2` to #3 via `gh issue edit`. | template-design-flaw | Manual edge added; 2nd hand-pass occurrence logged (n=2 across hand-passes). |
| M1 / Step 4 decompose | Workflow bash scripts are checked out CRLF (Windows `autocrlf`) yet ran clean under Git Bash. | shell-env | No action; latent risk flagged for scripts that string-compare git output. |
| M1 / Step 4 decompose | `new-handoff.sh` **and** `trigger-agent.sh` abort their repo-root guard on Windows Git Bash (`git rev-parse --show-toplevel` `C:/…` vs `pwd -P` `/c/…`). Blocks handoff-branch creation and wrapper-based dispatch. | shell-env / template-design-flaw | Worked around manually (branch by hand; dispatch decision to Product Owner); recommended narrow follow-up fix in **both** scripts. n=2 scripts, **blocking**. |
| M1 / Step 4 dispatch | Canary `codex exec` on Issue #4 confirmed Codex **runs** on Windows (non-interactive, `approval: never`) but failed **401 Unauthorized** ("Missing bearer or basic authentication"). No `~/.codex/auth.json`, no `OPENAI_API_KEY` — the Codex CLI is present but **unauthenticated** on the PC, so **all** agent dispatch is blocked until `codex login`. | machine (credentials) | Product Owner authenticates Codex (`codex login`), then re-dispatch. Exactly the toolchain drift the Mac→PC migration open decision predicted. **blocking**. |
| M1 / Step 4 dispatch | `codex exec --sandbox workspace-write -` (0.142.5) printed `sandbox: read-only` in its session header — the flag may not apply the way `trigger-agent.sh` assumes (it was validated on the donor's 0.139.0). | other (codex version drift) | To verify on the post-auth re-dispatch; if confirmed, `trigger-agent.sh`'s invocation needs a version-drift fix (e.g. `-c sandbox_mode=…`). Not yet confirmed (the run 401'd first). |
| M1 / Step 4 dispatch | **Resolved:** Product Owner ran `codex login` (ChatGPT). Re-dispatch on #4 succeeded and the header confirmed `sandbox: workspace-write [workdir, /tmp, $TMPDIR]` — so the earlier `read-only` was an unauthenticated-session artifact, **not** a `trigger-agent.sh` version-drift bug. | machine (resolved) | 401 blocker + read-only observation both cleared. |
| M1 / Step 4 dispatch | On Windows the codex `workspace-write` sandbox is **edit-only**: `.git` mounts read-only (`index.lock: Permission denied` → no commit/push), Git Bash won't start (`CreateFileMapping … Win32 error 5` → no bash verification), and there's no network (`gh` → connection refused). Codex produced the **correct** #4 diff but left it **uncommitted**; the SE verified it on the host and recorded it (PR #6). Far heavier than the donor's Mac baseline, where the agent committed + pushed and only PR-filing was manual. | machine / shell-env | Standing Windows limitation: `trigger-agent.sh` (even after #4) leaves verify/commit/push/PR to the human each dispatch. Forward decision to weigh: keep `workspace-write` (human does the git) vs `danger-full-access` (agent does the git, no sandbox). **n=1** — do not treat as template-general yet. |
| M1 / gate (Issue #2 → PR #7) | Planned gate recarve executed with **no new friction**: Codex authored `gate.py` + `pyproject.toml` + `.gitignore` additions in the edit-only sandbox; SE verified on host (`ruff` PASS, `pytest` PASS via exit-5-as-success, `GATE_EXIT=0`), committed attributed to Codex, opened PR #7. All friction was pre-logged at plan time (Part A gate rows); execution confirmed the hypotheses and added none. | bash-coupling (expected port) | Landed PR #7; author(Codex)/reviewer(SE) split held. Part A gate rows flipped planned→landed. |
| M1 / gate merge | PO reported PR #7 merged, but `gh pr view` showed it still **OPEN** (`mergedAt: null`; `origin/main` unmoved). SE trusted the artifact over the report, confirmed it was cleanly mergeable (no required checks), and completed the merge. | other (process) | Low signal, but a live win for **artifacts over memory**: GitHub state, not recollection, drove the call. |
| M1 / product orient — **falsification** | The Step-1 orientation claim ("`mediapipe.solutions.pose` feasible offline") was recorded from `import mediapipe` / `import cv2` **without ever executing `mediapipe.solutions.pose`**. First real product dispatch proved it false: installed `mediapipe==0.10.35` is a **Tasks-API-only** build — `import mediapipe` exposes only `Image`, `ImageFormat`, `tasks`; no `solutions` module exists. | other (product-dep) + **process** | **PROCESS finding**: an unverified feasibility assumption was logged as fact and survived to implementation. Lesson: verify a feasibility claim by executing the **exact** API, not the top-level import. Orientation row annotated as falsified. |
| M1 / product dispatch #1 (legacy handoff) | Codex wrote plausible `solutions.pose` code but — edit-only sandbox, **cannot run Python** — could not empirically check it. It correctly reasoned the API was absent, **reverted its own files to empty**, declared itself blocked, and returned the constraint conflict to the SE rather than switching to the forbidden Tasks API or fabricating. | machine/shell-env (sandbox) + **process (positive)** | Two findings: (i) **role boundary held under stress** — the implementer refused to violate scope and escalated; (ii) the edit-only sandbox means the implementer can't self-verify and must act on reasoning about the environment (here, correctly). Sandbox limitation routes to the PC-migration log (machine); the boundary behaviour is a positive process result. |
| M1 / product pivot + dispatch #2 (→ PR #8) | SE proved in scratchpad (no repo change) that the **Tasks API + a committed `.task` model** runs offline and yields 33 named landmarks; rewrote the handoff, committed the model asset (SE-provisioned, separate commit), re-dispatched. Codex produced a correct Tasks-API implementation; SE verified green (`gate.py`; CLI → 33 landmarks) and opened PR #8; PO merged. | other (product-approach pivot) | Product decision, **not** a template finding: M1's estimator API changed legacy→Tasks and a 5.8 MB model was committed (like the fixture image). Milestone shape/acceptance unchanged. |
| M1 / dogfood (post-#6) | `new-handoff.sh` created the #3 branch and `trigger-agent.sh` dispatched `codex exec` **twice** with no Windows repo-root abort, header confirming `sandbox: workspace-write`. | shell-env (resolved) | Confirms the #6 repo-root fix in real use; also closes the earlier "read-only header" version-drift question (never reproduced once authed). |
| M1 / retro (living-log note) | The six rows above (gate → product) were appended **at the retrospective**, reconstructed from this session's artifacts, not the moment each occurred — because the ledger lives on protected `main` while implementation happens on feature branches, making mid-branch ledger edits awkward. Planning-phase rows (Steps 1–4) *were* captured live. | process | Feeds PLAN.md Open Decision on strengthening the living-log convention before M2. |
| M2 / product dispatch (Issue #11) | Single clean dispatch: Codex authored `pose_comparison.py` + `tests/test_pose_comparison.py` to the precise normalization contract; **SE verified on host** — gate green (`ruff` PASS, `pytest` **7 passed**), CLI (fixture vs itself) → `0.0`, all 7 acceptance criteria met. No product friction. Codex *claimed* it also ran ruff/pytest in-sandbox ("7 passed" + a `.ruff_cache` write-permission warning) — but that is **unconfirmed**: it could equally be inferred from the 7 test functions it wrote and the identity→0 math. Per the M1 "verify by executing, not assuming" lesson, it is **not** logged as a sandbox-capability fact. | process (living-log pilot) + machine (unconfirmed) | **Logged live on the feature branch and folded into this PR** — the M2 living-log pilot working as intended (contrast the M1 retro row above). Open probe for a future dispatch: does the Windows `workspace-write` sandbox actually permit direct venv-Python execution? Confirm deliberately before nuancing the M1 edit-only finding. |
| M2 / merge | PO reported "merged 12" but `gh pr view` showed PR #12 still **OPEN** (`mergedAt: null`; `origin/main` at #10, issue #11 open, `pose_comparison` absent) — a **2nd occurrence** of the #7 "reported-merged-but-open" pattern. Caught because the retro's ledger read found the M2 dispatch row missing from `main`. SE completed the cleanly-mergeable PR. | other (process) | **Artifacts over memory, n=2:** a milestone-completion report was again contradicted by GitHub state; the ledger/PR check, not the report, drove the action. Worth watching whether the squash-merge UI step is reliably landing. |
| M2 / retro | Retro is light (contrast M1's first-hand-pass retro): M2 surfaced **no new template friction** — the warm harness (existing gate, dogfooded scripts) just worked. Resolves the M1 living-log open decision: the per-PR-append pilot **worked** (row written live, survived to `main`) → adopted as standing practice (caveat: one low-friction milestone). Role separation held again (n=2 on product code). Sandbox-self-execution stays an **open probe**. | process | This row itself is a legitimate retro-time observation (pilot success can only be judged after the fact); it is not reconstruction of missed live friction. PLAN.md Open Decisions updated accordingly. |
| M3 / product dispatch (Issue #15) | Clean single dispatch: Codex did the mechanical `_normalize_pose → normalize_pose` refactor (touching M2's shipped file) + authored `pose_feedback.py` + tests. **SE verified on host** — gate green (`ruff` PASS, `pytest` **14 passed** = M2's 7 + M3's 7, so the refactor did not regress scoring), CLI (fixture vs itself) → "Poses match.", all 7 acceptance criteria met. No product friction; the "refactor touches a shipped file" risk did not bite. **Sandbox-probe data point:** Codex reported running **nothing** this time — contrast M2, where it *claimed* a self-run of ruff/pytest. The agent's execution self-reporting is **inconsistent**, which reinforces that host verification is authoritative and the sandbox-exec probe stays open. | process (living-log) + machine (probe) | Logged live on the branch, folded into this PR. Refactor-safety confirmed by the gate running M2's tests unchanged. |
| M3 / retro | Retro again **light / warm-harness** (M3 low-friction like M2): **no new template friction**. First retro to *close/reframe* standing findings rather than only observe. (i) **Merge-execution finding RESOLVED** — the SE-runs-`gh pr merge` fix reached n=4 clean CLI merges (the closeout's n=3 incl. #16, plus #17's own merge); the reported-merged-but-OPEN gap (#7/#12/#13) has **not recurred** → downgraded from watch-item to **closed record**. (ii) **Living-log** held across M2+M3 (n=2) but **both low-friction** → "adopted as default," explicitly **NOT validated under load** (validated-in-principle ≠ validated-in-use); the first high-friction milestone is the real test. (iii) **Sandbox venv-Python probe** reframed **open probe → ACCEPTED CONSTRAINT, mitigated** via the friction-justification test: across M1–M3 the inconsistent execution self-reporting (see M3 dispatch row) cost **nothing concrete** — host verification compensates every time, so it sits on no critical path; the deliberate probe stays **OPTIONAL**. | process | PLAN.md Open Decisions updated (PR #17, PLAN.md-only). **Living-log wrinkle:** the retro's own ledger row was not folded into #17 (that PR was PLAN.md-only by instruction) — appended here as a deliberate follow-up, not an M1-style reconstruction from lost memory (the retro is fully recorded in #17). |
| M4 / product dispatch (Issue #20) | Clean single dispatch: Codex authored `pose_angles.py` (`joint_angles` → 8 vertex-named interior angles, `angle_feedback` → reference-relative open/close, thin CLI) + `tests/test_pose_angles.py` (8 tests) to the interior-angle contract — **new files only, no shipped file touched** (cleaner than M3's refactor). **SE verified on host** — gate green (`ruff` PASS, `pytest` **22 passed** = prior 14 + M4's 8, no regression), all 8 acceptance criteria met (angles in [0,180], 90°/180° geometry, **mirror invariance**, feedback sorted by \|delta\|, identity→blank, missing-landmark + coincident-ray `ValueError`). Interior-only slice held: **no `normalize_pose` call** (interior angles are normalization-invariant), no orientation angles. **Sandbox-probe data point:** Codex ran only PowerShell `Get-Content` reads + `apply_patch`, reported running no Python (edit-only) — consistent with the accepted-constraint framing; host verification authoritative. | process (living-log) + machine (probe) | Logged live on the branch, folded into this PR. **Third** low-friction product milestone in a row (M2/M3/M4) — living-log adopted-as-default still **not stress-tested under load**. |
| M4 / retro | Retro **light / warm-harness** (3rd straight low-friction product milestone): no new template friction; the narrowest-slice mitigation cut over-engineering at scoping and nothing crept back at implementation. Two findings surfaced. (i) **PR-filing conflict** — CLAUDE.md's "the filer is the Product Owner, not the Staff Engineer" vs. every PR (#8/#12/#16/#19/#21) filed by the SE under the PO's single `ChristopherHoward1` account; the "PO files" line is a donor/Mac **two-actor assumption** (agent pushes its own branch, separate human files) that doesn't port to a one-account setup with an edit-only sandbox. Held for PO decision (PLAN Open Decision); SE recommends rewording CLAUDE.md to match practice. (ii) **Offline-issue observation** — the Windows edit-only sandbox has no network, so the agent cannot read the GitHub issue (#20); its real input is the **self-contained handoff prompt**, and the issue serves humans/review only. True since M1, crystallized here. | process + template-design-flaw (edge) | (i) → PLAN Open Decision, awaiting PO; do not edit CLAUDE.md on SE judgment alone. (ii) → handoff prompts must carry the full contract on offline sandboxes; the Issue→agent-reads-issue step is Mac/online-shaped. |
